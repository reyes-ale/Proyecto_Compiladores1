
fn suma(a: i32, b: i32) {
    return a + b;
}

fn main() {
    let x = 10;
    let y = 20;
    let mensaje = "resultado de la suma";

    if x < y {
        let resultado = suma(x, y);
    } else {
        return;
    }

    while x > 0 {
        let mitad = x / 2;
    }
}